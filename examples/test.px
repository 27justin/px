import enum

calloc := @(extern: "C", import: "calloc") fn (_: u64, _: u64) -> ?any
printf := @(extern: "C", import: "printf") fn (_: !u8, ..) -> i32

main := fn () -> i32 {
  printf("%i, %i\n", system.A, system.B);
  0
}
