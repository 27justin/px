
let some_value: i32 = zero
var another_value: i32 = zero

printf := @(extern: "C", import: "printf") fn (_: !u8, ..) -> i32

main := fn () -> i32 {
  another_value = 42;
  printf("some_value = %i\nanother_value = %i\n", some_value, another_value);
  0
}
